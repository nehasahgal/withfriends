<!DOCTYPE html>
<html>
 
  
    <head>
      <title>WithFriends</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <%= javascript_importmap_tags %>

      <!-- Connect Bootstrap CSS -->
  <link rel="stylesheet" href="/stylesheets/bootswatch_lux.css">

  <!-- Connect Bootstrap JavaScript and its dependencies -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Connect Font Awesome -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/js/all.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1">


       
  
    
      <div class="bs-component">
        <nav class = "navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
          <div class="container-fluid">
            <a class="navbar-brand" href="/">WithFriends</a>
            <div class="collapse navbar-collapse" id="navbarColor01">
            
              <ul class ="navbar-nav me-auto">
                
                  <a class="nav-link active" href="/">All trips<span class="visually-hidden">(current)</span></a>
                
                
                  <% if current_user != nil %>
                    
                    <a class="nav-link active" href="/<%= current_user.id%>">My trips</a>
                    <a class="nav-link active" href="/users/edit">Edit profile</a>
                    <a class="nav-link active" href="/users/sign_out">Sign out</a>
                    
                  <%else%>
                    <a class="nav-link active" href="/users/sign_up">Sign up</a>
                    <a class="nav-link active" href="/users/sign_in">Sign in</a>
                  <%end%>
                </div>
                
          </div>
        </nav>
       </div>
      </head>

    <body>
     <div class="container-fluid">
          <%= yield %>
      </div>

    </body>
</html>
